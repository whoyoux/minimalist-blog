const Footer = () => {
	return (
		<footer className="w-full flex border-t py-3 items-center justify-center">
			<span className="text-muted-foreground text-sm">whx</span>
		</footer>
	);
};

export default Footer;
